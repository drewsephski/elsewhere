version: "3.9"
services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: elsewhere
      POSTGRES_PASSWORD: elsewhere
      POSTGRES_DB: elsewhere
    ports:
      - "5432:5432"
    volumes:
      - elsewhere_pg:/var/lib/postgresql/data
volumes:
  elsewhere_pg:
